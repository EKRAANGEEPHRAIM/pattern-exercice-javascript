// capitalize the first letter

function capitalize(str) {
    return str[0].toUpperCase() + str.slice(1);// slice is a  method that allowing to return a fragment of an existing  array
}

console.log(capitalize("manger")) // Manger