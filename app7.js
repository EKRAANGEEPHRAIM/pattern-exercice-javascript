// to convert a number into a positive number

// more optimized by using method Math.abs
function toPositive(number){
    return Math.abs(number);
}

console.log('the positive of this number is' , toPositive(-3))